# Local dev only. In Lambda, these come from the function's env vars (set by Terraform).
PORT=3000
ALLOWED_ORIGINS=http://localhost:5173
ITEMS_TABLE_NAME=portfolio-items
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
AWS_REGION=eu-north-1
